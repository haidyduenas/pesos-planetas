
if(planeta == "mercurio"){
var peso = mercurio*masa;
    mostrar("Hola" + nombre + ". Tu peso es  " + peso + " Klg en " + planeta);
}

if(planeta == "venus"){
var peso = venus*masa;
    mostrar("Hola" + nombre + ". Tu peso es  " + peso + " Klg en " + planeta);
}

if(planeta == "tierra"){
var peso = tierra*masa;
    mostrar("Hola" + nombre + ". Tu peso es  " + peso + " Klg en " + planeta);
}
